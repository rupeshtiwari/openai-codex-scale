# Prompt examples

Copy-paste-ready Codex prompts, one file per demo. Each prompt is bounded on purpose: it states
what to examine, what to produce, and what to leave alone.

A prompt that does not say what is out of scope will produce work that is out of scope.
